export const BrandName = ({ className = "" }: { className?: string }) => {
  return (
    <span className={`font-lgkw ${className}`}>
      Little Genius Kids World®
    </span>
  );
};

export const HubName = ({ className = "" }: { className?: string }) => {
  return (
    <span className={`font-hub tracking-wide ${className}`}>
      Innovation HUB
    </span>
  );
};
